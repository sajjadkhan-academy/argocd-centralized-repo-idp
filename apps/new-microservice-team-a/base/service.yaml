apiVersion: v1
kind: Service
metadata:
  name: new-microservice-team-a
  labels:
    app: new-microservice-team-a
spec:
  type: ClusterIP
  selector:
    app: new-microservice-team-a
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
