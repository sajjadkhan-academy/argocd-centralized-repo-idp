apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: test-service-a
  labels:
    app: test-service-a
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: test-service-a
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
