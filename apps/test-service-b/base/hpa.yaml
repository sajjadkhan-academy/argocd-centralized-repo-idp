apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: test-service-b
  labels:
    app: test-service-b
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: test-service-b
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
