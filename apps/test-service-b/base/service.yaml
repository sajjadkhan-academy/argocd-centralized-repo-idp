apiVersion: v1
kind: Service
metadata:
  name: test-service-b
  labels:
    app: test-service-b
spec:
  type: ClusterIP
  selector:
    app: test-service-b
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
