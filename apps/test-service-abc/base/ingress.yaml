apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-service-abc
  labels:
    app: test-service-abc
  annotations:
    nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
spec:
  ingressClassName: nginx
  rules:
    - host: test-service-abc.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: test-service-abc
                port:
                  number: 80
