apiVersion: v1
kind: Service
metadata:
  name: single-store-test
  labels:
    app: single-store-test
spec:
  type: ClusterIP
  selector:
    app: single-store-test
  ports:
    - name: http
      port: 80
      targetPort: 
      protocol: TCP
